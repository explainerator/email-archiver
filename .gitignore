/target
**/*.rs.bk

# DATABASE_URL contains the Postgres password
.env

# Generated by `terraform output -raw archiver_config`; holds live secrets
config.toml

# Ingest logs contain subjects, addresses and folder names
*.log

# Frontend build output. `dx` puts the wasm bundle under the crate's own target/.
web-ui/target/
